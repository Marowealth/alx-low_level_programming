#include "main.h"

/**
 * _puts - fuction that print a string follwed by a new line
 * Return: void
*/
void _puts(char *str)
{
	int index;
	
	for (index = 0; s[index] != '\0' ; index++);
	{
		_putchar(str[index]);
	}
	_putchar('\n');
}
