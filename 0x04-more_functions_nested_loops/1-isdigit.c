#include "main.h"

/**
 * mul - function to check a digit
 * @a: the first digit to check
 * @b: the second digit to check
 * Return: Always int
*/
int mul(int a, int b)
{
	if (a >= 0 && b <= 9)
	{
		return (1);
	}
	else
	{
	return (0);
	}
}
