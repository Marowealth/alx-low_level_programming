#include <stdio.h>

/**
 * main - print tebahpla.
 *Return: Always 0 (Success)
*/

int main(void)
{
	char alpha_rev

	for (alph_rev = 'z'; alph_rev >= 'a'; alph_rev--)
	{
		putchar(alph_rev);
	}

	putchar('\n');
	return (0);
}
