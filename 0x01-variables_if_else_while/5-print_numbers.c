#include <stdio.h>

/**
 * main - print digit of base 10
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int base;

	for (base = 0; base < 10; base++)
	{
		printf("%d", base);
	}

	printf("\n");
	return (0);
}
